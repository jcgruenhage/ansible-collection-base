---
requires_ansible: '>=2.17.0'

plugin_routing:
  modules:
    gpg_secretstore:
      deprecation:
        removal_version: "1.0.0"
        warning_text: Use famedly.base.openpgp_secretstore instead.
  lookup:
    gpg_secretstore:
      deprecation:
        removal_version: "1.0.0"
        warning_text: Use famedly.base.openpgp_secretstore instead.
